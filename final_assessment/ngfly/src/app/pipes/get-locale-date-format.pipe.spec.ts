import { GetLocaleDateFormatPipe } from './get-locale-date-format.pipe';

describe('GetLocaleDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLocaleDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
